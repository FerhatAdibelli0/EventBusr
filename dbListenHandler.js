const Logger = require("./eventBus");

Logger.emit("updatedData", "There is a change in database");

const Logger = require("./eventBus");

Logger.emit("updatedData", "There is a change in database");

const Logger = require("./eventBus");

Logger.emit("updatedData", "There is a change in database");

